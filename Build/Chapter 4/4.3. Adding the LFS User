 4.3. Adding the LFS User 
 Minty ~ # groupadd lfs
groupadd: group 'lfs' already exists
Minty ~ # useradd -s /bin/bash -g lfs -m -k /dev/null lfs
useradd: user 'lfs' already exists
Minty ~ # chown -v lfs $LFS/tools
changed ownership of '/mnt/lfs/tools' from root to lfs
Minty ~ # chown -v lfs $LFS/sources
changed ownership of '/mnt/lfs/sources' from root to lfs
Minty ~ # su - lfs
lfs@Minty ~ $ 


