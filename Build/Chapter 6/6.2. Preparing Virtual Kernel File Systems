6.2. Preparing Virtual Kernel File Systems

Minty benc # mkdir -pv $LFS/{dev,proc,sys,run}
mkdir: created directory '/mnt/lfs/dev'
mkdir: created directory '/mnt/lfs/proc'
mkdir: created directory '/mnt/lfs/sys'
mkdir: created directory '/mnt/lfs/run'
Minty benc # mknod -m 600 $LFS/dev/console c 5 1
Minty benc # mknod -m 666 $LFS/dev/null c 1 3
Minty benc # mount -v --bind /dev $LFS/dev
mount: /dev bound on /mnt/lfs/dev.
Minty benc # mount -vt devpts devpts $LFS/dev/pts -o gid=5,mode=620
mount: devpts mounted on /mnt/lfs/dev/pts.
Minty benc # mount -vt proc proc $LFS/proc
mount: proc mounted on /mnt/lfs/proc.
Minty benc # mount -vt sysfs sysfs $LFS/sys
mount: sysfs mounted on /mnt/lfs/sys.
Minty benc # mount -vt tmpfs tmpfs $LFS/run
mount: tmpfs mounted on /mnt/lfs/run.
Minty benc # if [ -h $LFS/dev/shm ]; then
>   mkdir -pv $LFS/$(readlink $LFS/dev/shm)
> fi
Minty benc #